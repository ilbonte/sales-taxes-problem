/*const expect = require('chai').expect;
const Cart = require('../src/Cart.js');

describe('Cart', function() {

    describe('calculate the total price', function(){
      it('return the price to be paid with tax included', function(){
        const cart = new Cart(['1 book at 12.49','1 music CD at 14.99','1 chocolate bar at 0.85']);
        cart.calculateTotal();
        expect(cart.total).to.be.equal(29.83);
        expect(cart.salesTaxes).to.be.equal(1.50);
      });

    });
});
*/
